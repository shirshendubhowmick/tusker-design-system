packages:
  - "apps/*"
  - "packages/*"
catalog:
  # Pin shared runtime peers so every workspace package resolves the same React.
  # Bump here (not per-package) when upgrading.
  react: 19.2.7
  react-dom: 19.2.7
  class-variance-authority: 0.7.1
allowBuilds:
  esbuild: true
  unrs-resolver: true
