/**
 * Same preview as interactive Storybook — theme pin still comes from
 * `import.meta.env.STORYBOOK_TEST_THEME` (Vite `define` in vitest.config.ts).
 */
export { default } from "../.storybook/preview";
